[project]
name = "cuats-hackathon-orderbook"
version = "0.1.0"
description = "A simple order book implementation with AVL tree."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
